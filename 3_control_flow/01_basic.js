// if - else if - else

// a || b || c

// switch --> if break not used then all case will be executed without further matching except default 

// falsy values-> false, 0, -0, BigInt 0n, "", null, undefined, NaN

// truthy-> "0",'false," ",[], {}, function(){}

/* Check Array if it's empty:
        if(array.length === 0){
        console.log("Array is empty")}

    Check if object is empty:
    const emptyObj={}
        if(Object.keys(emptyObj).lenght ===0 ){
        console.log("Object is empty")};
Imp. Case:

1. false == 0 // true
2. false == '' // true
3. 0 == '' // true

*/